# MongoDB Connection String (MUST provide a cloud URL like MongoDB Atlas for Render)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/scamshield

# Backend Port (Render sets this automatically)
PORT=10000

# Environment
NODE_ENV=production
